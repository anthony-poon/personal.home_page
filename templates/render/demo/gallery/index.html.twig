{% extends 'component/view/base.html.twig' %}

{% block css %}
    <link rel="stylesheet" href="{{ asset("build/demo/gallery/index.css") }}">
{% endblock %}

{% block javascripts %}
    <script src="{{ asset("build/demo/gallery/index.js") }}"></script>
{% endblock %}

{% block body %}
    <div id="gallery-container"
         data-ajax-url="{{ path("api_gallery_get_gallery_items") }}"
         data-is-logged-in="{{ is_granted("IS_AUTHENTICATED_FULLY") }}"
         data-sign-in-link="{{ path("security_login") }}"
    >
    </div>
{% endblock %}
